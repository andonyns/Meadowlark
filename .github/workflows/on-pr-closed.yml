name: On Pull Request Cancelled

on:
  pull_request:
    branches:
      - main
    types: [ closed ]

concurrency:
  group: ${{ github.event.pull_request.number}}
  cancel-in-progress: true

jobs:
  upgrade:

    name: Upgrade packages
    runs-on: ubuntu-latest
    steps:
      - run: echo "Cancelling in progress runs"
